# Oak & Ivory demo image sources
The UI uses stable Unsplash image URLs for the demo so the source package stays lightweight.
For production, download approved clinic photography into this folder and replace the URLs in page components.
